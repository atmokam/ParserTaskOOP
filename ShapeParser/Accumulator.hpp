#ifndef ACCUMULATOR_HPP
#define ACCUMULATOR_HPP

#include <string>
#include <vector>
#include <sstream>

class Accumulator {
public:
   
    static std::vector<std::string> accumulate(std::string input);
};

#endif